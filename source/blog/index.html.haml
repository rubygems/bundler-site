= partial 'partials/blog_header'

.row
  .container
    .col-md-12.content-padding
      %h4 Bundler news and updates

      %ul.posts
        - blog.articles.each do |article|
          %li
            .date= article.date.to_date.strftime("%b %e, %Y")
            = link_to article.title, article.url
