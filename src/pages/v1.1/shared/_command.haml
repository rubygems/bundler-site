%tr
  %td
    .cli_name
      = link("bundle #{name}", "./bundler_#{name}.html")
    .cli_desc
      = desc
  %td
    - opts.each do |opt|
      .cli_option
        = link("--#{opt}", "./bundle_#{name}.html##{opt}")
    .clear
